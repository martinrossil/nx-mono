{
  "name": "@bd/observables",
  "version": "0.0.0",
  "type": "module",
  "module": "lib/index.js",
  "scripts": {
    "dev": "tsup src/index.ts --outDir lib --format esm",
    "lint": "eslint src/**/*.ts",
    "build": "tsup src/index.ts --outDir lib --format esm",
    "test": "echo 'Testing observables...'"
  },
  "dependencies": {

  },
  "devDependencies": {
    
  }
}
