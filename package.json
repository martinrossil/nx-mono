{
  "name": "nx",
  "version": "0.0.0",
  "scripts": {
    "build": "npx nx run-many --target=build",
    "lint": "npx nx run-many --target=lint"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.4.0",
    "tsup": "^7.1.0",
    "typescript": "^5.1.5"
  },
  "workspaces": [
    "libs/*"
  ]
}
